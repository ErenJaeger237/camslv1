# Copy to .env.local and fill in for local dev
# In production, set these as environment variables in Vercel

# URL of the deployed FastAPI backend
# Leave empty to use Vite's proxy (http://localhost:8000)
VITE_API_URL=
